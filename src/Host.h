/**
 * \class Host
 */


#ifndef HOST_H
#define HOST_H
///Class used to create a server which initiates the connection among all the computers
class host{
public:
	///Helps to create all the peer to peer connections and gives back the total number of connected peers
	void gettotal(int);
};

//extern void ser();

#endif
